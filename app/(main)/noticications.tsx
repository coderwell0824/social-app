import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ScreenWrapper from "@/components/ScreenWrapper";

const Noticications = () => {
  return (
    <ScreenWrapper>
      <Text>Noticications</Text>
    </ScreenWrapper>
  );
};

export default Noticications;

const styles = StyleSheet.create({});
